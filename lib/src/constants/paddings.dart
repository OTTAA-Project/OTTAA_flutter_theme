import 'package:flutter/painting.dart';

class Paddings {
  static const sm = EdgeInsets.all(8);
  static const lg = EdgeInsets.all(20);
  static const xl = EdgeInsets.all(34);
  static const xs = EdgeInsets.all(12);
}